{
  "name": "accessflow-assets",
  "version": "1.0.1",
  "private": true,
  "description": "Tailwind build pipeline for AccessFlow's static assets.",
  "license": "MIT",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./app/static/app.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
