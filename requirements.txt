fastapi==0.141.1
uvicorn==0.52.0
# The two [standard] extras we actually want (fast event loop + HTTP parser).
# The rest of [standard] is dead weight here: websockets (unused protocol),
# watchfiles (--reload, dev only) and PyYAML (~6MB combined).
uvloop==0.22.1
httptools==0.8.0
sqlmodel==0.0.39
alembic==1.18.5
pydantic-settings==2.14.2
passlib[bcrypt]==1.7.4
itsdangerous==2.2.0
plexapi==4.18.2
python-telegram-bot==21.10
apscheduler==3.11.3
jinja2==3.1.6
python-dateutil==2.9.0.post0
bcrypt==4.0.1
cryptography==50.0.0
python-multipart==0.0.32
httpx==0.28.1

# Dropped: aiosmtplib — it was unused (mail_service uses stdlib smtplib), so
# removing it also removes CVE-2026-53533 from the tree.
#
# Dropped: babel — build-time only (pybabel compiles .po -> .mo in the Dockerfile's
# i18n stage). app/i18n.py uses stdlib gettext, so the 33MB of CLDR locale-data
# never ships. It stays in requirements-dev.txt for pybabel extract/update.
#
# cryptography bumped 44.0.1 -> 50.0.0 to fix GHSA-537c-gmf6-5ccf (HIGH,
# vulnerable OpenSSL in wheels) + CVE-2026-26007 (HIGH) + CVE-2026-34073 (LOW).
#
# Held back — new MAJOR available, needs review before bumping:
#   python-telegram-bot  21.10  -> 22.x  (major API changes)
#   bcrypt               4.0.1  -> 5.x   (major; keep in step with passlib[bcrypt] 1.7.4)
